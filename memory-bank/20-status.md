# 20 · 進度

最後更新：2026-09-05

## 完成咗（前端全部係假資料驅動）

- [x] 單檔 `index.html`，零建置零依賴，~1540 行
- [x] 追蹤：色鍵去背 → 影像矩 → 質心 / bbox / 主軸角 / 離心率
- [x] 圓形守衛：`ecc < 0.10` 時主軸畀 null（籃球唔會噴雜訊）
- [x] 輪廓：放射取樣 64 點 + EMA 平滑
- [x] **`projectFuture()` 2127 形態推演** —— 主旨嘅視覺實作
- [x] 改造模組：`CHIP` / `MONITOR` / `CONDUIT`，seed 驅動（錨點見下面「模組上身」）
- [x] 七節拍狀態機，轉一圈當進度條
- [x] 視覺 A/B/C/D：背景消融、泛光、文字生命（亂碼／打字機）、WebAudio 音效
- [x] **體積表現（2026-09-05）**：2127 輪廓疊 9 層剖面、外殼帶、前後軌道環、
      準星由整圈虛線改成四角括號、虛空由 TRON 網格改成深度環＋光柱＋三層視差塵埃
- [x] **捕捉環（2026-09-05）**：轉盤降格做掃描器 —— PROJECT 嗰圈錄低影像 + 逐格輪廓，
      MORPH 之後畫面播返個環，速度由 `CFG.playRpm` 話事，**同實體轉盤完全脫鈎**。
      接縫靠半徑指紋比對；nominal 由實測時間跨度推；實效格率太低（`< CFG.minFps`）自動退返 live。
      **實效格率 = `playRpm/60 × ringN`**，預設 90 格 × 20 RPM = 30fps
- [x] 顯示統一走 `#disp` canvas（`#feed` 退做解碼來源）→ live 同環共用一條路徑
- [x] **模組上身（2026-09-05）**：錨點由「射線索引＝固定畫面角度」改成**物體自身坐標
      `(方位角 θ, 高度 h)`**。yaw 就係環嘅格索引，所以貼片同影像數學上鎖實 ——
      貼片喺表面滑過、到邊緣壓扁、繞去背面消失、轉返出嚟。
      背面零件搬去 `#ovBack`（影像下層）→ **遮擋免費，零遮擋計算**
- [x] **真橫切面 + 表面網格（2026-09-05）**：一圈剪影 → shape-from-silhouette →
      真實 `R(高度, 方位角)`（`buildHull`）。貼片唔再靠橢球假設，交通卡唔會被當成圓柱；
      轉軸改用一圈質心平均。網格（緯線＝真橫切面、經線）剪落剪影度 →
      **物體本身睇落被重新分板造過**，而唔係外面圍咗啲光
- [x] 來源可插拔：`demo.mp4` / `?cam=1` / `?synth=1` / `?src=` / 拖放
- [x] 67 項自我檢查 `?test=1`
- [x] 測試素材 `demo.mp4`（綠幕旋轉籃球，H.264，實測 hue 120.2 / sat .99）

## 未做

### 後端 —— **一行都未寫**（最大缺口）

- [ ] Node `:7127` 行程
- [ ] `POST /api/project` —— Gemini 代理（金鑰唔可以入前端）
- [ ] `POST /api/print` —— ESC/POS 出單
- [ ] 前端要由 `mockScan()` 換成 `fetch('/api/project')`
- [ ] `package.json`、`.env.example` **都唔存在**（根 `README.md` §2.7 提過 `npm run dev` —— 假嘅）

### Gemini

- [ ] 真 prompt（persona 必須係**推演器**，唔係考古學家 —— 見 `00-intent.md`）
- [ ] `temperature` 開 1.1~1.3（唔開，同一件物品次次出差唔多嘅文案）
- [ ] **隱私守衛**：拒絕轉錄文件文字、拒絕人物／身體部位、拒絕證件形狀物
- [ ] 兩格擷取（0° / 180°）合併一次呼叫
- [x] ~~模組位置提示 `at:[x,y]`~~ —— 已入 schema，前端已接（見根 `README.md` §2.5）
- [ ] 離線罐頭：而家只有一個「推演失敗」，要 ~20 份對應通用分類

### 展示形式升級（2026-09-05 加入，規格見根 `README.md` §1.8）

- [ ] `?ghost=1` 輸出模式：純黑底 + 隱藏非發光面板 + 上下鏡像 + 校正偏移旋鈕
      —— **排最後做**（2026-09-05 決定）：要實體斜板先校得準，冇箱體寫咗都驗唔到
- [ ] Pepper's Ghost 箱體：45° 透明斜板、朝下顯示器、黑絨內裡、遮光罩
- [ ] `POST /api/led {beat, theme, rarity}` —— 燈帶跟七節拍，失敗要吞掉唔可以死
- [ ] 音效素材補齊 + 預載成 `AudioBuffer`（`kept` 嗰下係**靜音**，唔好加聲）
- [ ] 稀有度 `rarity = f(morph, moduleCount)` —— **由幾何算，唔可以用亂數**
- [ ] 輸出走收據定卡片：**第 1 日同印表機驗證一齊決定**（折衷：預印卡 + 感熱貼紙）

### 硬體 —— 全部未開始

- [ ] **印表機：買到並實測佢係咪 CDC／虛擬 COM**（最高風險，應該第一日做）
- [ ] 日文／中文要走點陣圖路徑（`GS v 0`），字碼頁冇漢字
- [ ] 電動轉盤 20 RPM
- [ ] LED 燈帶（WS2812B）+ 驅動
- [ ] 喇叭（展場好嘈，低頻要夠）
- [ ] 色鍵綠台座 + 弧形背板（45° 視角會影到物體後面）
- [ ] 相機約 45°（**唔好正俯拍** —— 剪影只會係純 2D 旋轉，形變量死掉）
      —— 裝好之後要校 `CFG.tilt`（預設 45）同 `CFG.spinDir`（`?spin=-1` 反轉）
- [ ] 曝光／白平衡鎖死（呢個架構下係承重項）

### 其餘

- [ ] 五種模組生成器未寫：`HEATSINK` `PORT` `ANTENNA` `CORE` `VENT`（而家全部當 `CHIP`）
- [ ] 出單版面：輪廓線稿 + 演化路徑 + `kept`
- [ ] `npm run calibrate`（抓台座色相、鎖曝光、定錨點）—— 只係 README 講咗
- [ ] `npm run soak`（第 7 日連續 50 次無人介入）—— 只係 README 講咗
- [ ] IDLE 吸引畫面除咗虛空網格之外冇嘢
- [ ] 網格濃淡（`.morphed #latRing/.62`、`#latLong/.42`、`CFG.latN/lonN`）未對住實體投影機調過

## 下一步（建議次序）

1. **印表機硬體驗證 + 收據／卡片二選一** —— 前置期最長、未知數最多。買錯型號成個出單管線要重做。
2. **Node `:7127`** —— 一次過解決金鑰外洩同印表機兩件事，解鎖之後所有嘢。
3. **Gemini 真 prompt + 隱私守衛** —— 隱私係最易變成真事故嗰項。
4. 補齊五種模組生成器。
5. 實體搭建（連 Pepper's Ghost 斜板對齊，留半日）+ `calibrate` / `soak`。

## 已知天花板（有意識嘅取捨，唔係 bug）

| 取捨 | 天花板 | 升級路徑 |
|---|---|---|
| 放射取樣輪廓（星形近似） | 凹陷會被橋接，鑰匙圈嘅孔畫唔到 | Moore 邊界追蹤 / Marching Squares |
| 外殼把薄物件當厚咗 | 誤差 ≈ `半闊 × tan(180°/n)`；90 個視角 → 半厚 6 還原成 7.05 | 加大 `CFG.ringN`（同時改善播放順暢度） |
| 外殼假設轉軸垂直 | 物件打橫瞓喺台座上，橫切面會變成好闊嘅扁圓盤 | 展場擺法令物件企起身 |
| `SLICES` 九層仲係同一條 2127 路徑縮放 | 唔係真橫切面 | 外殼接得上，但要順住 `projectFuture` 嘅生長量一齊算 |
| `MONITOR` 仲係浮空面板 | 唔係真貼喺表面 | 放棄細字、改用圖形讀數先做得到 |
| `CFG.spinDir` / `CFG.tilt` 靠人手校 | 轉向反咗會即刻穿崩 | 第 1 日裝好硬件對住實物校（`?spin=-1`） |
| 色鍵去背 | 透明／鏡面物體失敗 | 失敗率 >10% 先換 MediaPipe |
| 冇 WebGL | 光暈偏平 | `UnrealBloomPass`（唯一值得加 Three.js 嘅理由） |
| Pepper's Ghost 靠環境光壓得夠暗 | 展場燈太光 = 鬼影淡到睇唔到 | 遮光罩 + 加大顯示器亮度；再唔得就退返純螢幕模式 |
