# 組員展示講稿

啟動：`python3 -m http.server 8128 --bind 127.0.0.1`，開 `http://127.0.0.1:8128/demo.html`。
所有圖片／程式來自 repo，保持本地 server 運行即可；唔需要雲端 API。介面依 HAL 東京展示方向用日文。

## 三分鐘講法

1. **先體驗（約 45 秒）**：按「3 つの未来を自動で見る」。手機、鋼樽、交通卡逐一由原物件轉到 2127；每件先留 2.5 秒看原形，再看 7 秒未來。強調唔係加濾鏡：保留握座／瓶蓋／卡面，同時改變實體結構。可隨時停止、用滑桿比較。
2. **講展場價值（約 45 秒）**：觀眾自己帶入內容，有個人關係；形變可放大讓旁觀者一齊睇；運算本地完成，不靠場地網絡。呢啲係設計優勢，吸引力／人流仍要實地測試。
3. **留下結果（約 20 秒）**：按「2127 カードを見る」，展示直向 Object Card；輕微移動滑鼠會見到 tilt／glare。按「PNG を保存」下載同一張 750×1050 靜態卡，或者用 browser print 以 63×88mm 輸出；實體出卡未接 printer。
4. **證明有 pipeline（約 40 秒）**：右上「自分の写真で試す」，拖香蕉相／簡單背景單物件相。新相預設輪廓／顏色驅動嘅通用配方；專屬配方人手選，唔係 AI 自動認物。失敗後按「この写真を分離ラボで修正」，同一張相直接帶入 lab 調整，唔使重選檔案。
5. **收結（約 30 秒）**：打開「展示への次の一歩」。重點唔係預畫晒所有模型，而係將本地量到嘅輪廓交畀 renderer，再由 Gemini 回傳物件功能、保留／移除部位、附加結構、位置、色彩同信心度。識得就用專屬配方；唔肯定就用通用骨架；去背都唔穩就請觀眾修正或重拍。
6. **實體展望（約 20 秒）**：用參考影片解釋 Pepper’s Ghost：黑底明亮影像反射到透明板，看起來浮喺箱內，唔係真正空中投影。之後先接固定攝影箱、照明、透明板、打印同約 60 秒循環。

## 展示前

- 先開一次 demo 確認三件都能顯示；電腦接投影後用瀏覽器全螢幕。
- 自動導覽只播放一輪；切 tab 會停止，可再按播放。
- 相片只在瀏覽器處理；不要拿敏感證件或訊息畫面作示範。
- 透明、反光、與背景同色仍有去背限制；此版本不是 3D 重建、真實未來預測或硬件完成品。
